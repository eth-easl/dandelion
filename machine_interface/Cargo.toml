[package]
name = "machine_interface"
version = "0.1.0"
edition = "2021"

[build-dependencies]
cmake = "0.1"

[dependencies]
# FFI dependencies
libc = "0.2"
core_affinity = "0.8"
nix = "0.26"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
